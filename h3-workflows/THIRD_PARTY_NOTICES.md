# H3 distribution provenance / 来源与许可

- The workflow graphs derive from [Comfy-Org/workflow_templates](https://github.com/Comfy-Org/workflow_templates), MIT, copyright (c) 2023-present Comfy Org. Its full license is included as `WORKFLOW_TEMPLATES_LICENSE`. Panel manifests and integration changes use the repository MIT license.
- `ComfyRemoteH3PromptWriter` contains a workflow-only subset of a locally adapted [duckyshell/ComfyUI-MiniMaxH3-Prompt-Writer](https://github.com/duckyshell/ComfyUI-MiniMaxH3-Prompt-Writer), MIT, copyright (c) 2026 H3 Prompt Writer contributors. The full license is retained in that directory. Its original HTTP routes, web extension, local configuration and caches are excluded. The shared Ref2VA helper is imported locally instead of from a patched ComfyUI core; cache ownership is isolated to this package's process. This is not represented as an unmodified upstream release.
- `H3OfficialSkillPromptWriter`, `H3Ref2VAQwen35` and the model-independent `ref2va_contract.py` integration are released by the project owner under this repository's MIT license. `node-provenance.json` records normalized source and distribution hashes for the extracted source files; added integration files are marked separately. No private source repository, workstation paths or runtime state is included.
- Official prompt resources remain separately sourced from [MiniMax-AI/MiniMax-H3](https://github.com/MiniMax-AI/MiniMax-H3) and [MiniMaxAI/MiniMax-H3](https://huggingface.co/MiniMaxAI/MiniMax-H3). This distribution does not assert MIT rights over them and does not redistribute their text. `resources.json` pins source revisions and SHA256; `download_resources.py` retrieves and verifies them for installation. Consult upstream terms for those resources.
- ComfyUI, KJNodes, Ollama, model weights and LoRAs are external dependencies and retain their own licenses. Nothing here grants additional model-use or redistribution rights.

工作流基于 Comfy Org 的 MIT 模板；Prompt Writer 子集保留原 MIT 声明，并明确属于本地适配版本。项目所有者授权公开的独立节点及辅助模块采用本项目 MIT 许可。官方提示词资料不打包、不声明为 MIT，仅提供固定版本、哈希校验的独立下载方式。外部程序、模型和 LoRA 遵循各自许可。
